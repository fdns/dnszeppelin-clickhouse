sudo: required
language: go
services:
- docker
before_install:
  - docker-compose up -d
  - sudo apt-get update -qq
  - sudo apt-get install -y libpcap0.8 libpcap0.8-dev libnuma-dev
go:
  - 1.8
  - tip
